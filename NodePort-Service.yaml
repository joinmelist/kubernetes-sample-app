apiVersion: v1
kind: Service
metadata:
  name: wordpress
  namespace: capstone
spec:
  type: NodePort
  selector:
    app: wordpress
  ports:
  - port: 80   # service port
    targetPort: 80  # pod port
    nodePort: 30080 # node port



# kubectl port-forward svc/nginx-nodeport 30080:80